---
description: 将工作分解为小的、可验证的任务，包含验收标准和依赖排序
---

调用 agent-skills:planning-and-task-breakdown 技能。

阅读现有的规格说明（SPEC.md 或等效文件）和相关代码段。然后：

1. 进入计划模式——仅阅读，不修改代码
2. 识别组件之间的依赖关系图
3. 按垂直方向切片（每个任务一条完整路径，而非水平分层）
4. 编写带有验收标准和验证步骤的任务
5. 在各阶段之间添加检查点
6. 将计划提交给人工审查

将计划保存到 tasks/plan.md，任务列表保存到 tasks/todo.md。
